# TIPS

```vim

:vimtutor
" Mappings  
:Telescope keymaps

"  Call a lua function manually
:lua require('<FILE PATH FROM /lua>').function()

" Verbosity will show what is being executed by NeoVim
nvim -v9

" Refresh + clear cache + reset nvchad
rm -r ~/.config/nvim/plugin && rm -rf ~/.local/share/nvim && rm -rf ~/.cache/nvim && rm -rf /Users/vic8or/.local/state/nvim

```
